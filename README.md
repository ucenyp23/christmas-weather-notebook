Use simple-json-db, GraphQL API and SolidJS SPA for a weather notes (title, location, datetime, reported temp + unit, optional wind/precip), normalize and store temps in Celsius, compute feels-like tempriture, show user clothing and walk-duration advice, and award points per note with daily bonuses and special rewards for each day Dec 1–24. Runs locally, supports login, note creation/editing, shows current points, daily special reward calendar (Dec 1–24), immediate feedback after note creation (points earned + advice + today's special reward), unit toggle (C/F), offline-friendly caching/queueing for notes created offline, and sync when back online. Include: local-only run instructions (npm scripts) and .env.example.